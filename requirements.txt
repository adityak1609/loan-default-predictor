# Pinned to the versions the reported results were produced with (Python 3.10).
# Relaxed for Streamlit Cloud compatibility.
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
lightgbm>=4.3
shap>=0.41
matplotlib>=3.8
joblib>=1.3
pyarrow>=15.0
streamlit>=1.30
